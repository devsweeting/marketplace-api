entity: asset_media
processor: ../../dist/config/database/processors/MediaProcessor
items:
  mediaImage{1..200}:
    type: 'IMAGE'
    title: '{{lorem.words(4)}}'
    description: '{{lorem.words(4)}}'
    url: '{{image.imageUrl}}'
    asset: '@asset($current)'